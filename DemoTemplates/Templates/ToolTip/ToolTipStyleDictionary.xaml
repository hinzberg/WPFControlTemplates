<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="ForegroundColor" Color="Black"/>

    <SolidColorBrush x:Key="ContentBackground" Color="#DEE5F6"/>

    <SolidColorBrush x:Key="ContentBorderBrush" Color="#525E91"/>

    <ControlTemplate x:Key="ToolTipTemplate" TargetType="ToolTip">
        <Border Name="Border" BorderThickness="1" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
                Background="{StaticResource ContentBackground}" BorderBrush="{StaticResource ContentBorderBrush}">
            <ContentPresenter HorizontalAlignment="Center" Margin="4" VerticalAlignment="Top"/>
        </Border>
    </ControlTemplate>

    <Style x:Key="{x:Type ToolTip}" TargetType="ToolTip">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Foreground" Value="{StaticResource ForegroundColor}"/>
        <Setter Property="FontFamily" Value="Arial"/>
        <Setter Property="FontSize" Value="14px"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template" Value="{StaticResource ToolTipTemplate}"/>
    </Style>

</ResourceDictionary>