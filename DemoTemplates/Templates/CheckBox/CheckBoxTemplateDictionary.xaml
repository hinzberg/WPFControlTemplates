<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Templates/_Colors/CheckBox/CheckBoxColorDictionary.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="CustomCheckBoxTemplate" TargetType="{x:Type CheckBox}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <BulletDecorator Grid.Column="0">
                <BulletDecorator.Bullet>
                    <Grid Width="13" Height="13">

                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"/>

                        <!--<Ellipse x:Name="CheckMark" Stroke="{DynamicResource MarkBrush}" Fill="{DynamicResource MarkBrush}" Width="10" Height="10"></Ellipse>-->
                        <Path x:Name="CheckMark" Fill="{StaticResource CheckBoxMarkBrush}" Stroke="{StaticResource CheckBoxMarkBrush}" HorizontalAlignment="Center"
                              VerticalAlignment="Center"  StrokeThickness="0.1" Data=
                             "M 6.5,11 
                          C   6,11 -3,3    6,8 
                          C   6,8  15,-6 6.5,11"/>
                    </Grid>
                </BulletDecorator.Bullet>
            </BulletDecorator>

            <ContentPresenter Grid.Column="1" 
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                    Margin="{TemplateBinding Padding}" 
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                    RecognizesAccessKey="True"/>

        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="false">
                <Setter Property="Visibility" Value="Collapsed" TargetName="CheckMark"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Background" Value="{StaticResource CheckBoxMouseOverBrush}" TargetName="Border"/>
            </Trigger>

            <Trigger Property="IsPressed" Value="true">
                <Setter Property="Background" Value="{StaticResource CheckBoxPressedBrush}" TargetName="Border"/>
                <Setter Property="BorderBrush" Value="{StaticResource CheckBoxPressedBorderBrush}" TargetName="Border"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" Value="{StaticResource CheckBoxIsEnabledBackground}" TargetName="Border"/>
                <Setter Property="BorderBrush" Value="{StaticResource CheckBoxIsEnabledBorder}" TargetName="Border"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

</ResourceDictionary>